import 'package:flutter/material.dart';

/// Centralized color constants for the Our Moment app.
class AppColors {
  const AppColors._();

  // ── Brand ──
  static const primary = Color(0xFF655CBB);

  // ── Background ──
  static const scaffoldBackground = Color(0xFFF2F2F7);
  static const white = Color(0xFFFFFFFF);
  static const buttonGrey = Color(0xFFF5F5F7);
  static const segmentBackground = Color(0xFFF1F1F3);

  // ── Text ──
  static const textPrimary = Color(0xFF1D1D1D);
  static const textDark = Color(0xFF1E1E1E);
  static const textSecondary = Color(0xFF6C7278);

  // ── Icon ──
  static const iconInactive = Color(0xFFC1C1C1);

  // ── Border & Divider ──
  static const border = Color(0xFFE0E0E0);

  // ── Input ──
  static const inputHint = Color(0xFFACACAC);

  // ── Semantic ──
  static const danger = Color(0xFFFF3B30);

  // ── Notification type colors ──
  static const notifCeremonyIcon = Color(0xFF655CBB);
  static const notifCeremonyBg = Color(0xFFF4F1FF);
  static const notifCeremonyBorder = Color(0xFFD9D1FF);

  static const notifReminderIcon = Color(0xFFF2994A);
  static const notifReminderBg = Color(0xFFFFF4EA);
  static const notifReminderBorder = Color(0xFFFAD8B5);

  static const notifUpdateIcon = Color(0xFF2D9CDB);
  static const notifUpdateBg = Color(0xFFEDF8FF);
  static const notifUpdateBorder = Color(0xFFCBEAFF);

  static const notifOfferIcon = Color(0xFF27AE60);
  static const notifOfferBg = Color(0xFFEFFAF3);
  static const notifOfferBorder = Color(0xFFCBEED7);
}
